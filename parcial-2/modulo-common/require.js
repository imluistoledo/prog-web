//let areas = require("./areas.js")

import * as areas from "./areas.js";
let cuadrado = areas.areaCuadrado(4)
console.log(cuadrado)

import areaOfCircle from "cal_area_of_a_circle" 

    const result = areaOfCircle(45)
    console.log(result)